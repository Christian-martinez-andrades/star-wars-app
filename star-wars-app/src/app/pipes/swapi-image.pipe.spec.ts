import { SwapiImagePipe } from './swapi-image.pipe';

describe('SwapiImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SwapiImagePipe();
    expect(pipe).toBeTruthy();
  });
});
